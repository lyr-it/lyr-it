<p class="claim">The best way to learn new languages</p>
<% if (session.name) { %>
  <p class="userId">Hello
    <%= session.name %>
  </p>
  <% if (typeof(songs) !== 'undefined') { %>
    <div class="titleSong">
      <p class="nameArtists">
        <%= songs.nameArtists %>,
          <%= songs.nameSong %>
      </p>
    </div>
    <div class="lyrics">
      <pre><%= songs.lyricSong %></pre>
    </div>
    <div class="lyricsTrans">
      <pre><%= songs.translateLyricSong %></pre>
    </div>
    <% } %>
      <% } else { %>
        <p>user disconected</p>
        <% } %>
          <a href="/logout" class="logout">Logout</a>
